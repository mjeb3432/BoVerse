# Copy this file to .env.local and fill in your keys. .env.local is gitignored.

# ─── LLM provider ──────────────────────────────────────────────────────────
# Google Gemini 2.0 Flash — primary LLM. Free tier: 15 req/min, 1M tokens/day.
# Get a key: https://aistudio.google.com/app/apikey
GOOGLE_GENERATIVE_AI_API_KEY=

# Groq Llama 3.3 70B — optional backup for speed-critical paths.
# Get a key: https://console.groq.com/keys
GROQ_API_KEY=

# ─── Postgres (Supabase) ───────────────────────────────────────────────────
# Use the "Connection pooling" string (port 6543) on serverless platforms
# like Vercel — direct connections to Supabase aren't designed for high
# fan-out, the pooler handles that for us.
#
# Find it in Supabase: Project Settings → Database → Connection string
# → "Transaction" pooler mode.
DATABASE_URL=
